image: node:9.11.1

pages:
  cache:
    paths:
    - elm-stuff//

  script:
  - npm install -g elm --unsafe-perm=true
  - elm make src/Main.elm --optimize --output=public/elm.js
  artifacts:
    paths:
    - public
  only:
  - master
